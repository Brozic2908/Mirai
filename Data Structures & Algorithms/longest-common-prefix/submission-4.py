class Solution:
    def longestCommonPrefix(self, strs: List[str]) -> str:
        result = ""

        print(strs[0])
        
        # for i in range(len(strs)):
        #     for c in strs[i]:

        return result